package com.butchercraft.world.checkpoint;

/** Canonical identities for checkpoint publication causes outside the live trigger enum. */
public final class CheckpointTriggerIdentities {
    public static final String LEGACY_WORKSTATION_PROJECTION_SUCCESSOR =
            "butchercraft:checkpoint_trigger/legacy_workstation_projection_successor";

    private CheckpointTriggerIdentities() {
    }
}
